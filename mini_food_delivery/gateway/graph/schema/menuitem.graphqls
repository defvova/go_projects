type MenuItem {
    id: ID!
    categoryId: ID!
    name: String!
    description: String
    imageUrl: String
    available: Boolean!
    createdAt: Time!
}

extend type Query {
    menuItems(categoryId: ID!): [MenuItem]!
}
